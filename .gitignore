# Ignore the contents of the _site directory and other cache directories
_site/
.sass-cache/

# Ignore the directory for local files during development
local/

# Ingore files created by npm
node_modules
package-lock.json

# macOS
.DS_Store

# Bundler: gems are installed into the project, not committed
.bundle/
vendor/

# iCloud sync conflict copies (this repo lives under ~/Desktop)
* 2.*
